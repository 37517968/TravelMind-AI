# Phase 0 基线报告（2026-09-19）

## 环境

- 操作系统：Windows；
- Java：JDK 21.0.8；
- Maven：仓库内 Apache Maven 3.9.9；
- Spring Boot：3.4.4；
- Spring AI：1.0.0；
- Docker：本次执行时 daemon 不可用。

## 改造前构建基线

- 使用 JDK 21 和仓库内 Maven Local Repository 执行 `compile`：通过；
- Maven Wrapper 默认受到当前环境 `HOME/JAVA_HOME` 配置影响，直接执行会尝试写入 `C:\.m2\repository`，因此本报告使用显式 JDK 21 和仓库内缓存。

## Phase 0 正确性基线

使用 JDK 21 执行 Maven 测试，结果如下：

- 测试总数：6；通过：5；失败：0；错误：0；跳过：1；
- RAG 离线样例：5 条，`Recall@3 = 1.000`；
- 本地检索平均耗时：`15.728 ms`（最后一次完整复测）。该数值只用于代码回归，不代表 HTTP 接口或生产环境性能；
- Flyway MySQL Testcontainers：因 Docker daemon 不可用自动跳过，迁移脚本尚需在可用 Docker 环境再次验证；
- 完整 Spring Context：不作为 Phase 0 默认测试，因为会绑定真实数据库、模型和 MCP 配置，后续建立独立 test profile。

## 性能基线状态

当前没有运行中的隔离测试环境、固定模型配额和稳定网络，因此不产出虚假的 HTTP QPS、P95 或模型耗时。本阶段只建立测量口径；Phase 2 异步任务完成后进行正式 API/Worker 压测。

## Token/成本基线状态

当前 ChatClient 调用链尚未统一采集模型 Usage，也没有在测试环境调用真实付费模型。本阶段记录为 `NOT_MEASURED`，不能在简历中声称 Token 降幅。Phase 1 上下文模块应先接入分段 Token 统计，再执行压缩前后对照实验。

## 限制

- 本地 hash embedding 仅用于不依赖外部 API 的可运行基线，不代表真实语义向量质量；
- 5 条 RAG 样例只能作为回归烟雾测试，正式评测集需扩充到 100～300 条；
- 本报告中的本地耗时不作为生产容量规划依据。
