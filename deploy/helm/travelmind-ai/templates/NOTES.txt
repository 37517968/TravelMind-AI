1. API service:
   {{ include "travelmind-ai.fullname" . }}-api:8123
2. Verify rollout:
   kubectl rollout status deployment/{{ include "travelmind-ai.fullname" . }}-api
   kubectl rollout status deployment/{{ include "travelmind-ai.fullname" . }}-agent-worker
   kubectl rollout status deployment/{{ include "travelmind-ai.fullname" . }}-knowledge-worker
3. The Secret {{ .Values.existingSecret }} is external and is never generated by this chart.
